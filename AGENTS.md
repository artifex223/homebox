# Homebox — Agent Notes

Preferred host: workstation

This repo's toolchain (frontend build, typecheck, browser tests) is heavy JS work; soulforge is a 4 GiB-class LXC and cannot run it without starving other services on the same host.
